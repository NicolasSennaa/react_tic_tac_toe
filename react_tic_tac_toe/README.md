# 🕹️ Jogo da Velha (Tic-Tac-Toe) em React

### Sobre o Projeto

Este projeto é uma implementação do clássico **Jogo da Velha (Tic-Tac-Toe)**, desenvolvido utilizando o framework React.

O principal objetivo deste repositório foi colocar em prática conceitos fundamentais do React:
* **Componentização** e como os componentes se comunicam (hierarquia).
* Gerenciamento de **Estado** de forma centralizada (`useState`).
* Uso de **Props** e **Callbacks** para manipular eventos (como o clique nas casas).
* O conceito de **"Time Travel"** (viagem no tempo), que permite desfazer jogadas através do histórico de estados.

Este projeto demonstra como o React lida com a complexidade de um jogo através da imutabilidade e do levantamento de estado (*Lifting State Up*).

### 🛠️ Tecnologias Utilizadas
* **React** (Criado via `create-react-app`)
* **Node.js / npm**
* **JavaScript (JSX)**

### Como Rodar o Jogo Localmente
Para testar o jogo na sua máquina:

1.  **Clone o repositório:**
    ```bash
    git clone [https://github.com/NicolasSennaa/react_tic_tac_toe.git](https://github.com/NicolasSennaa/react_tic_tac_toe.git)
    cd react_tic_tac_toe
    ```
2.  **Instale as dependências:**
    ```bash
    npm install
    ```
3.  **Inicie o servidor de desenvolvimento:**
    ```bash
    npm start
    ```

O jogo será aberto automaticamente no seu navegador, geralmente em `http://localhost:3000`.

### 📄 Licença
Este projeto está sob a Licença **Creative Commons** (CC0 1.0 Universal). Sinta-se à vontade para inspecionar, aprender e reutilizar o código.